# --- Core agent stack ---
langgraph>=0.2.50
langchain-anthropic>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- Memory backends ---
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary,pool]>=3.2.0
redis>=5.0.0

# --- Vector search (events RAG) ---
pgvector>=0.3.0

# --- HTTP layer ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# --- External APIs ---
googlemaps>=4.10.0
httpx>=0.27.0

# --- Firebase (ID-token verification for the chat email-verified gate) ---
firebase-admin>=6.5.0

# --- Web crawl fallback (worldwide off-platform events) ---
# Crawl4AI is optional at runtime — the agent runs without it, only the
# live-crawl fallback path is disabled. Install only on environments that
# can run a headless browser (Playwright/Chromium).
crawl4ai>=0.4.0

# --- Utilities ---
python-dotenv>=1.0.0
structlog>=24.4.0

# --- Dev / test ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.6.0
