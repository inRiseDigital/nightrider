"""Shared prompt fragments — boilerplate every agent imports.

Currently empty. Add common policy text here (e.g. tone guide, refusal
policy, cultural sensitivity reminder) and import it into per-agent prompts.
"""

POLICY = """Be concise. Be culturally aware. Never share a user's location
or RSVP without explicit confirmation. Default to politeness."""
