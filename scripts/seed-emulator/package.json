{
  "name": "seed-emulator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fills the local Firebase emulator (Auth + Firestore + Storage) with realistic Night Ride data matching docs/FIRESTORE_SCHEMA.md, for developing the Flutter app and the webpanel against.",
  "scripts": {
    "seed": "node seed.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
